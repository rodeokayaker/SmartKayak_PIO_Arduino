# Интеграция фильтра относительной ориентации

## Описание решения

Создана комплексная система для точного определения относительной позиции весла относительно каяка с защитой от магнитных помех и использованием цикличности гребли.

## Основные компоненты

### 1. **Детектор магнитных помех**
- Анализирует дисперсию магнитного поля за последние 20 измерений
- Проверяет резкие изменения магнитуды (>20% = помеха)
- Вычисляет динамический уровень доверия (0.0 - 1.0)
- **Принцип**: используем магнитометр ТОЛЬКО когда уверены в данных

### 2. **Адаптивный комплементарный фильтр**
- DMP кватернионы (гироскоп + акселерометр) - основа (98% веса)
- Магнитометр - коррекция дрифта yaw (2% веса, но только при отсутствии помех)
- Автоматическая адаптация весов в зависимости от уровня доверия

### 3. **Анализатор паттернов гребли**
- Запоминает до 10 характерных паттернов гребков
- Для каждого паттерна сохраняет:
  - Изменение yaw за гребок
  - Начальный и конечный угол шафта
  - Длительность гребка
  - Сторону лопасти
- Предсказывает положение весла на основе похожих гребков

### 4. **Коррекция на основе цикличности**
- Во время гребка: корректирует ориентацию на основе паттерна
- Между гребками: полагается на DMP + избирательно магнитометр
- Накапливает статистику для улучшения точности

## Интеграция в SmartKayak

### Шаг 1: Добавьте фильтр в SmartKayak.h

```cpp
#include "RelativeOrientationFilter.h"

class SmartKayak {
    // ... существующие поля ...
    
private:
    RelativeOrientationFilter orientationFilter;
    uint32_t lastFilterUpdateTime;
    
    // ... остальной код ...
};
```

### Шаг 2: Инициализация в конструкторе

```cpp
SmartKayak::SmartKayak():
    // ... существующая инициализация ...
    lastFilterUpdateTime(0)
{
    // ... существующий код ...
}
```

### Шаг 3: Калибровка при старте

Добавьте метод для калибровки когда каяк стабилен:

```cpp
void SmartKayak::calibrateMagnetometer() {
    // Вызывать когда каяк неподвижен и нет помех
    // Например, перед стартом заезда
    orientationFilter.calibrateReference();
}
```

### Шаг 4: Замените getRelativeOrientation

Модифицируйте метод `update()`:

```cpp
void SmartKayak::update() {
    // ... существующий код до получения ориентации ...
    
    // Получаем DMP кватернионы
    SP_Math::Quaternion currentPaddleQ(
        paddleOrientation.q0, paddleOrientation.q1, 
        paddleOrientation.q2, paddleOrientation.q3
    );
    
    SP_Math::Quaternion currentKayakQ = kayakOrientationQuat;
    
    // Получаем данные магнитометров
    IMUData kayakIMU;
    imu->getData(kayakIMU);
    IMUData paddleIMU = paddle->getIMUData();
    
    SP_Math::Vector kayakMag(kayakIMU.mag_x, kayakIMU.mag_y, kayakIMU.mag_z);
    SP_Math::Vector paddleMag(paddleIMU.mag_x, paddleIMU.mag_y, paddleIMU.mag_z);
    SP_Math::Vector kayakGyro(kayakIMU.gx, kayakIMU.gy, kayakIMU.gz);
    SP_Math::Vector paddleGyro(paddleIMU.gx, paddleIMU.gy, paddleIMU.gz);
    
    // Вычисляем dt
    uint32_t currentTime = millis();
    float dt = (currentTime - lastFilterUpdateTime) / 1000.0f;
    lastFilterUpdateTime = currentTime;
    
    // ОСНОВНОЙ ВЫЗОВ ФИЛЬТРА
    SP_Math::Quaternion paddleRelativeQuat = orientationFilter.update(
        currentKayakQ,
        currentPaddleQ,
        kayakMag,
        paddleMag,
        kayakGyro,
        paddleGyro,
        dt
    );
    
    // Определяем сторону лопасти
    BladeSideType bladeSide = getLowerBladeSide(currentPaddleQ, paddle->getBladeAngles().YAxisDirection);
    currentBladeSide = bladeSide;
    
    // ... получаем силу и другие данные ...
    
    // Обновляем состояние гребка для фильтра
    bool strokeActive = (abs(bladeForce) > borderLoadForce);
    float shaftRotationAngle, shaftTiltAngle, bladeRotationAngle;
    getPaddleAngles(paddleRelativeQuat, shaftRotationAngle, shaftTiltAngle, bladeRotationAngle);
    
    orientationFilter.updateStrokeState(
        strokeActive,
        bladeSide,
        shaftRotationAngle,
        bladeForce
    );
    
    // ... остальной код использует paddleRelativeQuat ...
}
```

### Шаг 5: Добавьте диагностику

```cpp
void SmartKayak::printOrientationDiagnostics() {
    float kayakTrust, paddleTrust;
    orientationFilter.getMagnetometerTrust(kayakTrust, paddleTrust);
    
    uint32_t interferenceCount, correctionCount;
    orientationFilter.getStats(interferenceCount, correctionCount);
    
    Serial.printf("🧭 Магнитометр каяка: доверие=%.2f\n", kayakTrust);
    Serial.printf("🧭 Магнитометр весла: доверие=%.2f\n", paddleTrust);
    Serial.printf("📊 Помехи: %lu, Коррекций: %lu\n", interferenceCount, correctionCount);
}
```

## Преимущества решения

### ✅ Защита от помех
- Автоматическое обнаружение магнитных помех
- Плавное переключение на гироскоп при помехах
- Нет резких скачков ориентации

### ✅ Использование цикличности гребли
- Запоминает паттерны типичных гребков
- Предсказывает положение на основе истории
- Самообучение с каждым гребком

### ✅ Точность
- DMP кватернионы - краткосрочная точность (гироскоп)
- Магнитометр - коррекция долгосрочного дрифта (только при отсутствии помех)
- Паттерны - коррекция на основе биомеханики

### ✅ Адаптивность
- Автоматическая настройка весов фильтра
- Учет качества каждого источника данных
- Накопление опыта для улучшения точности

## Рекомендации по использованию

### 1. Калибровка
```cpp
// При старте системы (каяк на воде, неподвижен)
void setup() {
    // ... инициализация ...
    
    delay(2000); // Ждем стабилизации
    smartKayak.calibrateMagnetometer();
    
    Serial.println("Калибровка завершена!");
}
```

### 2. Мониторинг качества
```cpp
void loop() {
    smartKayak.update();
    
    // Каждые 5 секунд выводим диагностику
    static uint32_t lastDiag = 0;
    if (millis() - lastDiag > 5000) {
        smartKayak.printOrientationDiagnostics();
        lastDiag = millis();
    }
}
```

### 3. Рекалибровка при необходимости
```cpp
// Если заметили дрифт - можно рекалибровать
if (buttonPressed) {
    Serial.println("Рекалибровка...");
    smartKayak.calibrateMagnetometer();
    orientationFilter.resetStats();
}
```

## Параметры тюнинга

В `RelativeOrientationFilter.h` можно настроить:

```cpp
#define MAG_VARIANCE_THRESHOLD 800.0f   // Чувствительность к помехам
                                        // Больше = менее чувствительно
                                        
#define YAW_DRIFT_CORRECTION_RATE 0.02f // Скорость коррекции дрифта
                                        // Больше = быстрее коррекция
                                        
#define GYRO_INTEGRATION_WEIGHT 0.98f   // Вес гироскопа в фильтре
                                        // 0.98 = 98% гироскоп, 2% магнитометр
                                        
#define STROKE_PATTERN_MEMORY 10        // Количество запоминаемых паттернов
```

## Алгоритм работы

```
1. Получаем DMP кватернионы (надежные краткосрочно)
   ↓
2. Проверяем магнитометр на помехи
   ↓
3. Если нет помех → используем для коррекции yaw дрифта
   ↓
4. Комплементарный фильтр: DMP + магнитометр (с адаптивными весами)
   ↓
5. Вычисляем относительную ориентацию весла
   ↓
6. Если идет гребок → корректируем на основе паттерна
   ↓
7. Сохраняем паттерн гребка для будущего использования
```

## Результат

Вы получаете:
- **Стабильную** относительную ориентацию без скачков
- **Точную** коррекцию на основе физики гребли
- **Защиту** от магнитных помех
- **Самообучающуюся** систему, улучшающуюся с опытом

Фильтр автоматически выбирает лучший источник данных в каждый момент времени!
